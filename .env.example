# FabricStudio API Configuration
# Copy this file to .env and update with your values

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Server Secret (CRITICAL - Required for encryption)
# Generate with: openssl rand -base64 32
FS_SERVER_SECRET=your-secret-key-here-change-this

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Hostname to bind to (default: 0.0.0.0)
HOSTNAME=0.0.0.0

# Port to listen on (default: 8000)
PORT=8000

# Enable HTTPS (default: false)
# Set to true to enable HTTPS
HTTPS_ENABLED=false

# SSL Certificate paths (only needed if HTTPS_ENABLED=true)
SSL_CERT_PATH=/app/certs/cert.pem
SSL_KEY_PATH=/app/certs/key.pem

# =============================================================================
# FABRICSTUDIO API CONFIGURATION
# =============================================================================

# Primary FabricStudio host
LEAD_FABRIC_HOST=https://your-fabricstudio-host.com

# OAuth Client ID
CLIENT_ID=your-client-id

# OAuth Client Secret
CLIENT_SECRET=your-client-secret

# =============================================================================
# CORS CONFIGURATION (Optional)
# =============================================================================

# Comma-separated list of allowed origins
# If not set, origins are auto-generated from HOSTNAME, PORT, and HTTPS_ENABLED
# Example: CORS_ALLOW_ORIGINS=http://localhost:8000,https://example.com
# CORS_ALLOW_ORIGINS=

# =============================================================================
# MICROSOFT TEAMS INTEGRATION (Optional)
# =============================================================================

# Microsoft Teams webhook URL for sending Adaptive Card notifications
# When a report is created, an Adaptive Card will be sent to this Teams channel
# 
# To get a webhook URL:
#   1. In Microsoft Teams, go to your channel
#   2. Click the "..." menu next to the channel name
#   3. Select "Connectors"
#   4. Search for "Incoming Webhook"
#   5. Click "Configure"
#   6. Give it a name (e.g., "FabricStudio Reports")
#   7. Click "Create"
#   8. Copy the webhook URL and paste it here
#
# If not set or empty, Teams notifications will be silently disabled (no errors or warnings)
# Example: TEAMS_WEBHOOK_URL=https://outlook.office.com/webhook/abc123-def456-ghi789/...
TEAMS_WEBHOOK_URL=

# =============================================================================
# ADVANCED CONFIGURATION (Optional - Defaults shown)
# =============================================================================

# Database Configuration
# DB_PATH=data/fabricstudio_ui.db
# DB_TIMEOUT=30.0
# DB_MAX_RETRIES=5
# DB_RETRY_DELAY=0.1
# DB_MAX_CONNECTIONS=20
# DB_BACKUP_RETENTION_DAYS=7

# Rate Limiting
# RATE_LIMIT_REQUESTS=100
# RATE_LIMIT_WINDOW=60

# Session Configuration
# SESSION_KEY_TTL=3600
# MAX_SESSION_KEYS=1000

# Cache Configuration
# REPO_CACHE_TTL_HOURS=1
# TEMPLATE_CACHE_TTL_HOURS=1

# SSH Configuration
# SSH_OPERATION_TIMEOUT=300

# Frontend Configuration
# FRONTEND_REQUEST_TIMEOUT=30000

# Cleanup Configuration
# CLEANUP_EXECUTION_RETENTION_DAYS=90
